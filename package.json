{
  "name": "dom-styler",
  "version": "1.0.0",
  "description": "A simple tool to apply styles to dom-elements.",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "babel --watch --presets es2015 src -d build",
    "build": "babel --presets es2015 src -d build"
  },
  "author": "Maximilian Sauermann",
  "license": "ISC",
  "dependencies": {
    "to-camel-case": "^1.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0"
  }
}